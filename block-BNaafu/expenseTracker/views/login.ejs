<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('partials/header') %> 
    <main>
        <section class="h-custom flex flex-col items-center w-1/3 mx-auto justify-center">
            <form action="/users/login" method="POST" class="flex flex-col p-8 border-2 border-gray-300 rounded-lg w-custom">
                <% if(error) { %>
                    <legend class="text-red-500 font-bold"><%= error %></legend>
                <%} %>
                <% if(info) { %>
                    <legend class="text-green-500 font-bold"><%= info %></legend>
                <%} %>
                    <input type="email" name="email" placeholder="Enter Email" class="p-3 bg-gray-200 my-2 rounded-lg border-2 border-gray-300">
                    <input type="password" name="passwd" placeholder="Enter Password" required class="p-3 bg-gray-200 my-2 rounded-lg border-2 border-gray-300">
                    <input type="submit" value="Login" class="p-3 bg-blue-500 text-white font-bold my-2 rounded-lg hover:bg-blue-400">

                <div class="text-center mt-3">
                    <a href="/users/login/forgotpassword" class="text-blue-700 text-xl">Forgotten Password?</a>
                </div>
               
            </form>
            <div class="mt-8 flex justify-between w-custom">
                <a href="/auth/github" class="bg-green-600 hover:bg-green-500 p-3 rounded-lg text-white font-bold text-xl">Login with GitHub</a>
                <a href="/auth/google" class="bg-green-600 hover:bg-green-500 p-3 rounded-lg text-white font-bold text-xl">Login with Google</a>
            </div>
        </section>
    </main>
   
   
</body>
</html>